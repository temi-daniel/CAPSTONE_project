apiVersion: v1
kind: Service
metadata:
  name: project-app
  namespace: default
  labels:
    app: project-app
spec:
  type: LoadBalancer
  selector:
    app: project-app
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
